{
  "name": "kalai-backend",
  "version": "2.0.0",
  "description": "Kalai food tracking backend — Bun + SQLite",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "import": "bun run scripts/import.ts"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
